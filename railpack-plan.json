{
  "build": {
    "preInstall": [
      "apt-get update",
      "apt-get install -y libcairo2-dev libfreetype6-dev libffi-dev libxml2-dev libxslt-dev libpango1.0-dev"
    ],
    "install": [
      "python -m venv /app/.venv",
      "pip install -r requirements.txt"
    ],
    "build": [
      "pip install -r requirements.txt && python manage.py migrate --noinput && python manage.py collectstatic --noinput"
    ]
  },
  "deploy": {
    "start": "gunicorn TaskOptimizer.wsgi:application"
  }
}